ansible-ubuntu_up2date
======================

Ansible role to keep a Ubuntu installation up to date.

By default, it does the following:

- Do a safe upgrade to the latest (stable) version
- Install and configure unattended-upgrades
